def stableInternships(interns, teams):
    for intern in interns:
        for idx in range(len(interns[0])):
            intern[idx]

    return []



interns = [[0, 1], [1, 0]]
teams = [[1, 0], [1, 0]]
stableInternships(interns, teams)