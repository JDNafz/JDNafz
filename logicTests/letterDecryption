const readline = require("readline");

function convertToSum(inputString, setTotal, difference) {
  // Convert the input string to a sum of corresponding numbers using ASCII values
  const totalSum = inputString.split("").reduce((sum, char) => {
    const charValue = char.toLowerCase().charCodeAt(0) - "a".charCodeAt(0) + 1;
    return sum + (charValue >= 1 && charValue <= 26 ? charValue : 0);
  }, 0);
  if (setTotal === "x") {
    return totalSum + difference;
  } else difference = setTotal - totalSum;
  return difference;
}

// const inputStr = "OCCAM";
// const setTotal = "x";
// const difference = 3;

// if (setTotal === "x") {
//   console.log(
//     `Given ${inputStr} + ${difference}, the answer is:`,
//     convertToSum(inputStr, setTotal, 3)
//   );
// } else {
//   console.log(
//     `The difference from'${inputStr}' is:`,
//     convertToSum(inputStr, setTotal)
//   );
// }

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout,
});

rl.question("Please enter the inputString: ", (inputStr) => {
  rl.question(`Please enter the answer or 'Enter' if unknown:`, (answer) => {
    if (answer === "") {
      rl.question(`Please input the operation (i.e. +3 or *4)`)
      console.log(
        `Given ${inputStr} + ${difference}, the answer is:`,
        convertToSum(inputStr, setTotal, 3)
      );
    }
    rl.close();
  });
});
rl.question("Please enter the first value: ", (firstInput) => {
  rl.question("Please enter the second value: ", (secondInput) => {
    console.log(`You entered the first value: ${firstInput}`);
    console.log(`You entered the second value: ${secondInput}`);
    rl.close();
  });
});
